/**
 * if
 * else-if
 * else
 */

/**
 * Khi nào dùng if-else:
 * 1. Ít trường hợp
 * 2. Khi không biết trước được giá trị cụ thể
 * 3. Khi logic tính toán điều kiện trở lên phức tạp
 */

/**
 * Khi nào dùng switch-case:
 * 1. Khi có nhiều tình huống xảy ra và có 1 trường hợp duy nhất
 * 2. Khi biết trước giá trị của từng trường hợp
 * 3. Khi cần gộp các trường hợp giống nhau
 */
